{
  "name": "remix-wizard",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/rimraf": "^4.0.5",
    "eslint-config-custom": "*",
    "make-dir": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "prettier": "latest",
    "rimraf": "^5.0.0",
    "shx": "^0.3.4",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "npm@8.5.0"
}
